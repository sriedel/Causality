require 'spec_helper'

describe Causality::Collector do
  describe "#initialize" do
    it "should connect with the queue"
  end

  describe "#push" do
    context "the queue is up" do
      it "should store the passed event to the queue"
    end
  end

end
